{
  "name": "gmail-analyser",
  "description": "Analyse emails in a Gmail inbox",
  "scripts": {
    "test": "jest --silent",
    "push": "clasp push",
    "run": "clasp run",
    "logs": "../../scripts/watch-logs.sh",
    "login": "clasp login --creds ../../creds.json"
  }
}
